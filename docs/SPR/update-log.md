# Update Log

Tracks pulls from origin for this workspace.

| Date (local) | Origin HEAD at pull | Latest origin commit | Note |
|---|---|---|---|
| 2026-09-15 | 74e7a4be5 | 74e7a4be5 2026-09-15T03:32:50+00:00 docs: update weekly stars chart | Fetched; local master still 2 ahead / 45 behind (diverged). Not yet merged. |
| 2026-09-15 | 74e7a4be5 | (same) | Merged origin/master into local master (merge commit 251aa7015). No conflicts. Local 2 commits preserved unpushed. |
| 2026-09-15 ~21:32Z | — | — | Started pre-release verification `cargo test -p jcode-tui --lib` (task 570185h4gp). Next steps queued: on green, bump version to 0.85.0, commit `chore(release): prepare v0.85.0`, tag v0.85.0. Holding for test completion. |
| 2026-09-15 ~23:03Z | — | — | Wedged test task 570185h4gp cancelled (no output in 84 min, output file missing). Fresh `cargo test` run found lib-test compile break: `ModelRoute.usage` added upstream by ff329733f without updating test initializers. Same break confirmed on clean origin/master. Fixed locally (5db3f3790). |
| 2026-09-15 ~23:40Z | — | — | Full test verification: 8 lib failures + 21 workspace-crate failures are identical on clean origin/master (env-dependent, pre-existing). Local commits add zero regressions. service_tier passthrough committed (36e00ba20). |
| 2026-09-15 ~23:43Z | — | — | `chore(release): prepare v0.85.0` (51eb6d8e2): Cargo.toml 0.84.0→0.85.0, Cargo.lock, changelog/v0.85.0.json. Tag v0.85.0 created locally. Push attempt failed: danflory has no write access to 1jehuang/jcode (403, push: false, no fork). Release deferred permanently: workspace declared personal no-push (poisoned push URL NO_PUSH_PERSONAL + unconditional pre-push hook). |
| 2026-09-16 ~00:04Z | — | — | Built and installed local binary via `scripts/install_release.sh --fast`: v0.85.0-dev (51eb6d8e2, dirty) → ~/.jcode/builds, launcher → ~/.local/bin/jcode. |
| 2026-09-16 ~00:33Z | — | — | Research + implementation: DeepInfra service tier visibility (SPR-0003). Restrict passthrough to flex/priority, log exact outgoing tier per request, parse response service_tier echo, surface via ServiceTier stream event → ServerEvent → TUI `Tier:` row. Verified: workspace check clean, runtime tests 134 passed, message-types/openrouter tests green. Commit ad2c923f5. |
| 2026-09-16 ~01:01Z | — | — | Rebuilt and reinstalled: v0.85.1-dev (ad2c923f5, dirty) live in ~/.local/bin/jcode. |
| 2026-09-16 ~00:20Z | — | — | SPR-0004 opened and closed: DeepInfra wire tier stuck at flex despite config standard. Root cause: `JCODE_OPENAI_EXTRA_BODY={"service_tier":"flex"}` in `~/.config/jcode/deepinfra.env` (added 2026-09-14 20:19 EDT), merged after the config eviction. Fix: line removed (backup `deepinfra.env.pre-standard-20260915`), stale comment stripped (backup `.pre-comment-strip`), code hardened so the tier decision applies LAST (commit 4804febd9) with 2 fail-first regression tests (136 passed). Binary 4804febd9-dirty installed; production server restarted and verified `REQUEST SERVICE_TIER: omitted (standard)`. |
| 2026-09-16 ~08:10Z | — | — | SPR-0005 opened and closed: visible swarm spawn sessions were never persisted, so the spawned terminal's `jcode --resume <id>` died with "No session found matching ...". Root cause: `Session::save()` deliberately skips brand-new empty sessions (guard in persistence.rs), but `create_visible_spawn_session` hands the session to a separate process that must resolve it by id on disk. Fix: new `Session::save_forced()` (opt-out of the skip policy) used at the visible-spawn call site (commit a7b6411bd), with a unit + regression test (77 jcode-base + 7 visible-spawn tests green). |
| 2026-09-16 ~12:49Z | — | — | SPR-0005 activated: shared server repointed from pre-fix 51eb6d8e2-dirty to 572f2eb0d and daemon reloaded (selfdev build-reload). Visible spawns now persist their session snapshot so --resume attaches. Doc status updated (d56abcc97). |
| 2026-09-16 ~15:57Z | — | — | SPR-0006: created personal GitHub fork `danflory/jcode` (fork of `1jehuang/jcode`), re-wired remotes (`origin`->`upstream` fetch-only + poisoned push, new `personal` fork remote), created and pushed local `dev` branch (23 custom commits) to the fork. Rewrote pre-push hook to block official-upstream pushes while allowing the personal fork. Official repo remains push-proof. |
| 2026-09-16 ~17:33Z | — | — | SPR-0007 closed: folded the sandbox clone's `WithLocalMods` auth fix ("configured OpenAI-compatible profiles not counted as available auth", original `7a6ea59ef`) into the unified `dev` branch as cherry-pick `677462389`. Also committed the operator's personal-release build guide (`build-for-personal-release.md`), added the generic SPR `README.md`, and recorded this SPR-0007. All operator work from `WithLocalMods` is now on the single `dev` branch and pushed to the `personal` fork; the sandbox clone was reconciled onto `dev` (safety backup `backup-with-local-mods` kept). |
| 2026-09-17 ~21:38Z | — | — | SPR-0008 opened (sessionCorruption): client-local session state overrides server session identity and working directory. Confirmed two symptoms on the shared-server topology (six collaborating sessions with swarms): (A) resuming session `t-rex` from a client whose cwd was a sibling clone silently rewrote its persisted `working_dir` from `/home/d/dev_env/clones/Overwatch_2` to `/home/d/dev_env/clones/Overwatch` (server log + pre-wipe backup evidence); (B) `/info` on a remote client reported a client-local stub session (`Session: snail (session_)`) instead of the server-bound session. Root causes: an unconditional `working_dir_override` on resume (`client_lifecycle.rs` -> `restore_session_with_working_dir`), and `/info` reading `app.session` directly while three sibling paths already prefer `remote_session_id`. Branch `sessionCorruption` created from `dev`. Fix not yet implemented; narrow-first plan in the SPR. |
| 2026-09-18 | 490584f03 | 490584f03 Merge sessionCorruption (SPR-0008 fixes) into dev | Merged `sessionCorruption` into `dev` (`--no-ff`, merge commit `490584f03`); `sessionCorruption` fully contained, branch retained and not deleted. Verified both carried changes are additive SPR-0008 fixes (resume-cwd precedence in `turn_execution.rs`, `/info` server identity in `state_ui.rs`); `cargo check` on `jcode-base`/`jcode-app-core`/`jcode-tui` clean. Pushed `dev` to `personal`. Sandbox clone moved off `sessionCorruption` onto `dev` and fast-forwarded to `490584f03` (both machines now in sync); sandbox `origin` remote switched from HTTPS to SSH so pushes work unattended. |
| 2026-09-18 | 490584f03 | 490584f03 | SPR-0010 opened: unstable session context prefix — volatile timestamp in the first provider-visible message defeats prompt cache reuse across swarm workers. Confirmed by direct measurement: longest common prefix across 13 worker sessions is **60 characters** in all three model groups (`V4.1-Flash`, `V4-Flash-0731`, `V4-Flash`), truncating mid-`Time:` line. Root cause: `build_session_context` (`prompt.rs:699`) emits `session_datetime_lines()` third, ahead of every stable field, inside a block whose stated purpose is prefix stability ("immutable session-context snapshot as the first provider-visible transcript item"). Cost evidence: role-split hit rates measured at 95.7% root / 90.9% child-`V4-Flash-0731`; worker break-even for switching tiers is 97.58%. Also corrected a `model_cost_replay` `mix` accounting defect that reports 48.8% aggregate hit rate by treating `input_tokens` as fully uncached (true rates are 88–96%). Folder `docs/SPR/SPR-0010-unstable-session-context-prefix-cache/` created (anchor + Deficiency Report + TP Change Report + Cost Evidence + Fix Explanations). Fix specified but **not yet applied**. |
| 2026-09-18 | abe799030 | abe799030 | SPR-0010 observability implemented: `JCODE_TRACE=1` now dumps the exact rendered request payload (commit `b89aeaf6d`). New `Agent::dump_request_prefix` (`prompting.rs`) called at both send points (`turn_loops.rs`, `turn_streaming_mpsc.rs`), emitting system static/dynamic, tool schemas with a `name:desc_len` fingerprint, and each leading message with role+length. Gated on the **existing** `JCODE_TRACE` switch — a `features.dump_request_prefix` config field was prototyped and reverted once the existing knob was found, so no new config surface. Verified live: a real request produced a readable dump showing the session-context block with volatile `Time:` at line 3, confirming the defect directly. Two new volatile fields found in the block that the static read missed: `Git:` (branch + `Modified: N files` list, per-tree-state) and `Jcode version:` (includes build dirtiness). Doc updated. Fix itself still not applied. |
| 2026-09-18 | a9a141aef | a9a141aef | SPR-0010 fix applied (commit `9d0efd403`): `build_session_context` now emits the stable block first (OS, Architecture, Jcode version, hardware, working directory, git info) and the datetime lines last. **Verified on real payloads** via the `JCODE_TRACE` dump: common prefix between two sessions rose **60 -> 617 chars**, divergence moving from mid-`Time:` (line 3) to the end of the block. New test `test_session_context_orders_volatile_fields_last` **fail-first checked** (fails on the old ordering, passes after). 34 `prompt_tests` green, existing context test unmodified. SPR status CONFIRMED -> FIXED; V-1/V-2/V-3 satisfied. **V-4 outstanding**: post-fix worker cache hit rate re-measurement on a live two-worker swarm run. |
| 2026-09-18 | a007b44aa | a007b44aa | SPR-0010 V-4 verified on **live swarm workers**: two real workers spawned via the swarm tool (`session_t-rex_1789762270422`, `session_tiger_1789762274439`), both on `deepseek-ai/DeepSeek-V4-Flash-0731`, both at `v0.85.64-dev (a007b44aa)`. Worker-to-worker common prefix = **474 chars** (baseline 60), diverging only at `Time:` at the end of the block. Fix confirmed on the actual swarm path, not just isolated sessions. CORRECTED: an earlier note here claimed the daemon was stale on `572f2eb0d`. That was wrong. `ps` start-time metadata suggested Sep 16, but `/proc/<pid>/exe` resolves to `versions/a007b44aa/jcode` and `jcode server reload` reports the newest binary is already running. The daemon is on the fixed build; no restart was required. Post-fix worker cache hit-rate re-measurement still pending a longer worker run. |
| 2026-09-18 | 1e1209ba5 | 1e1209ba5 | Operator decision: unify on `deepseek-ai/DeepSeek-V4.1-Flash` for **both** coordinator and workers. Applied on both machines: host `[agents] swarm_model` changed from `DeepSeek-V4-Flash-0731` to `DeepSeek-V4.1-Flash` (backup `config.toml.bak-same-model-4.1-1789762650`); sandbox had no `swarm_model` key and gained one set to the same value (backup taken). Verified live: a spawned worker reports `model: deepseek-ai/DeepSeek-V4.1-Flash` and a post-fix context ordering. **Recorded evidence conflict, for the record:** the measured worker baseline was 88.6% cache hit rate on 2525 turns against a 97.58% tier break-even, below which `V4.1-Flash` workers cost more on the child mix (~$0.47 vs $0.35 on the child sample). The operator is monitoring actual costs directly, which supersedes the projection. Revisit if observed costs rise. |
